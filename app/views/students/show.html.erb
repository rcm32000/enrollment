<%= button_to "Edit", edit_student_path(@student), method: 'get' %>
<%= button_to "Delete", student_path(@student), method: "delete" %>

<h1>Student Name</h1>
<%= @student.name %>

<%= render partial: 'addresses/form' %>
<%= render partial: 'students/address', collection: @student.addresses %>
