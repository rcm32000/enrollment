<h1>All Students</h1>
<% @students.each do |student| %>
  <div id="student-<%= student.id %>" >
    <a link_to ><%= student.name %></a>
    <%= button_to "Edit", edit_student_path(student) %>
    <%= button_to "Delete", student_path(student), method: "delete" %>
  </div>
<% end %>
